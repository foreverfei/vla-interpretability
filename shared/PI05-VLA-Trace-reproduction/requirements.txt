# ============================================================
# VLA-Trace π0.5 Reproduction — Python Dependencies
# ============================================================

# ------------------------------------------------------------------
# Core ML framework
# ------------------------------------------------------------------
torch>=2.0
numpy>=1.24,<2.0
safetensors>=0.4
transformers>=4.40
sentencepiece>=0.2

# ------------------------------------------------------------------
# openpi — π0.5 model definition (Physical Intelligence)
# ------------------------------------------------------------------
jax>=0.4
jaxlib>=0.4
flax>=0.8
tensorflow>=2.15
absl-py

# ------------------------------------------------------------------
# LIBERO-10 benchmark environment (robosuite + MuJoCo)
# ------------------------------------------------------------------
robosuite>=1.4
libero==0.1.1
mujoco>=2.3,<3.0
mujoco-python-viewer
free-mujoco-py

# ------------------------------------------------------------------
# HuggingFace Hub — model download (uses hf-mirror.com in China)
# ------------------------------------------------------------------
huggingface_hub>=0.20

# ------------------------------------------------------------------
# Video / image processing
# ------------------------------------------------------------------
imageio>=2.30
imageio-ffmpeg>=0.4
opencv-python-headless>=4.8
pillow>=10.0
matplotlib>=3.7

# ------------------------------------------------------------------
# VLA-Trace (installed from local git clone)
#   cd ~/autodl-tmp/VLA-Trace && pip install -e .
# ------------------------------------------------------------------

# ------------------------------------------------------------------
# Utilities
# ------------------------------------------------------------------
pyyaml>=6.0
scipy>=1.10
